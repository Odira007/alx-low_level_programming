#include "main.h"

/**
 * mul - Writes a function that multiplies two integers
 * @a: first argument
 * @b: second argument
 * Return: mul(a * b)
 */
int mul(int a, int b)
{
	int mul;

	mul = a * b;
	printf("%d\n", mul);
	return (mul);
}
