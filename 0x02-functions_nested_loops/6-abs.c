#include "main.h"

/**
 * _abs - absolute value
 *
 * Description: print the absolute value of a number
 *
 * Rerurn: 0
 */
int _abs(int n)
{
	if (n < 0 || n > 0)
	{
		return (n);
	}
	else
		return (0);
}
