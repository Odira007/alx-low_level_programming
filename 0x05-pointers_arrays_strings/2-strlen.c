#include "main.h"
#include <stdio.h>

/**
 * _strlen - function that returns the length of a string
 * @s: parameter
 * Return: always 0
 */
int _strlen(char *s)
{
	int len;

	len = strlen(*s);
	return (0);
}
